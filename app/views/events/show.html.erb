<div class="container">
  <p>
    <h2><%= @event.title%></h2>
  </p>
  <p>
    <h3><%= @event.description%></h3>
  </p>

<button type="button" class="btn btn-warning"><%= link_to 'Edit', edit_event_path(@event)  %></button>
<a href="<%= event_path(@event.id)%> class="btn btn-warning" class="btn btn-danger" data-confirm="êtes vous sur?" data-method="DELETE" >Supprimer</a>

</div>